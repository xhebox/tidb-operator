apiVersion: pingcap.com/v1alpha1
kind: TidbCluster
metadata:
  name: basic
spec:
  version: v6.3.0
  timezone: UTC
  pd:
    baseImage: pingcap/pd
    replicas: 1
    maxFailoverCount: 0
    requests:
      storage: "1Gi"
    config: {}
  tikv:
    baseImage: pingcap/tikv
    replicas: 1
    maxFailoverCount: 0
    requests:
      storage: "1Gi"
    config:
      storage:
        reserve-space: "0MB"
      rocksdb:
        max-open-files: 256
      raftdb:
        max-open-files: 256
  tidb:
    baseImage: tidb
    imagePullPolicy: Never
    version: latest
    replicas: 1
    maxFailoverCount: 0
    config: {}
  tiproxy:
    baseImage: tiproxy
    imagePullPolicy: Never
    version: latest
    replicas: 1
    requests:
      storage: "1Gi"
